--- project-doc ---
@codex/入口.md

- 只为非显然业务规则、核心不变量、复杂分支和取舍原因补中文注释；注释解释“为什么”，不复述代码。简单逻辑通过清晰命名表达。
- Git 提交说明使用中文，推荐：`模块：动作 + 目的`。
- 不添加无明确需求的兼容或静默兜底；前置条件不满足时明确报错。
- 仅在有并行收益且写入 Scope 不重叠时使用子代理；主代理负责交付。
