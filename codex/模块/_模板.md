# {模块名称}

> 保存为就近 `AGENTS.md` 或 `ai_constraints/codex/模块/{模块名称}.md`，同一模块只保留一份。建议 25–45 行，硬上限 80 行；人工确认内容只写 manual 区块。

<!-- ai-constraints:generated:start -->
## 目标与边界

- **目标**：[稳定能力与用户]
- **负责**：[职责]
- **不负责**：[边界]

## 核心不变量与验证

| 核心不变量 | 实现入口 | 验证证据 |
|---|---|---|
| [代码或测试已证实的规则；不足写 UNKNOWN] | `[真实相对路径或符号]` | `[测试文件或可执行命令]` |

## 公开契约与直接消费者

- **公开契约**：[接口、schema、事件、共享状态；纯内部模块写“无”]
- **直接消费者**：[真实相对路径；不确定写 `UNKNOWN`，后续改动最低按 R3]
- **副作用 / 失败语义**：[仅保留不能从签名直接读出的稳定事实]

## 修改风险

- [最可能破坏不变量或消费者的位置与原因]

## 验证命令

- **快速**：`[来自现有任务脚本或 CI 的精确命令]`
- **完整**：`[最终门禁或高风险切片命令]`
- **核心场景**：[主路径、边界与失败路径]
<!-- ai-constraints:generated:end -->

<!-- ai-constraints:manual:start -->
<!-- ai-constraints:manual:end -->
