# {模块名}

> AI 改本模块前必读。软上限 80 行，超出说明模块该拆了。

**路径**：`src/xxx/`
**职责**：[一句话]
**状态**：`[已实现]` / `[部分实现]` / `[开发中]` / `[待开发]`

---

## 对外接口

| 函数 / 路由 | 入参 | 出参 | 说明 |
|------------|------|------|------|
| `xxx` | `{ foo, bar }` | `{ data }` | ... |

---

## 依赖关系

- **依赖**：[模块 A]（用于...）、[第三方库 X]（用于...）
- **被依赖**：[模块 C]、[模块 D]
- **外部服务**：[如有]

---

## 内部约定 / 本模块特有的坑

> 仅记录本模块特有内容。跨模块通用的坑放到 `ai_constraints/codex/坑点.md`。

- [条目]

---

## 测试

- 测试文件：`src/xxx/xxx.test.ts`
- 跑命令：`npm test -- xxx`
- 核心用例：
  - [用例描述]

---

*发现“其他模块改本模块时踩过的坑”，先判断是否是全局问题；全局问题记到 `坑点.md`，本模块私有问题留在这里。*
