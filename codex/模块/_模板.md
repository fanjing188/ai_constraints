# {模块名称}

> 将本模板复制到模块代码目录的 `AGENTS.md`。模块跨多个目录时，保存为 `codex/模块/{模块名称}.md`，并在 `codex/架构.md` 登记唯一入口。
> 档案只记录理解模块时最昂贵且稳定的信息，建议控制在 40 至 80 行。

## 目标与边界

- **解决的问题**：[为用户或系统提供什么能力]
- **负责**：[模块职责]
- **不负责**：[明确排除的职责]

## 代码入口

- **主入口**：`src/...`
- **公开接口**：`src/...`
- **数据 / 状态**：`src/...`
- **测试**：`tests/...`

## 主流程

1. [关键步骤，只描述业务流程]
2. [关键步骤]
3. [关键步骤]

## 核心不变量

| 不变量 | 实现入口 | 验证证据 |
|---|---|---|
| [绝不能被修改破坏的规则] | `[符号或路径]` | `[测试文件或手工场景]` |

## 对外契约

- **输入**：[关键输入和约束]
- **输出**：[关键输出和错误语义]
- **依赖模块**：[模块和用途]
- **被依赖模块**：[调用方]
- **外部服务 / 副作用**：[存储、消息、网络或文件写入]

## 状态与数据

- [关键状态及允许的迁移]
- [数据一致性、幂等、金额、时间或权限规则]

## 修改风险

- [最容易产生回归的位置及原因]
- [需要同步检查的调用方或异步流程]

## 验证

- **快速命令**：`[项目已有的精确命令]`
- **完整命令**：`[高风险变化需要的命令]`
- **核心场景**：[主路径、关键边界、失败路径]

## 模块特有坑点

- [只记录已经发生、根因明确且可能复发的问题]

## 维护规则

- 档案更新条件和内容边界统一按 `codex/入口.md` 的“模块档案同步”执行。
