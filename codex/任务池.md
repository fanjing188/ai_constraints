# 活跃任务锁

> 仅在多个活跃 Owner、多窗口并行、跨会话交接或共享热点文件存在冲突风险时使用。
> 单 Owner 的普通修改不读取、不写入本文件。这里不是需求池、进度报告或长期档案。

## 使用规则

- 开始并发写入前登记 Owner、写入 Scope 和状态；只允许 Owner 修改自己的任务行。
- Scope 重叠时不得并行写入，先调整边界或将冲突任务标为 `阻塞`。
- 子代理不修改本文件；由主代理登记其占用的写入 Scope。
- 完成、取消或交接结束后立即删除任务行，不归档聊天过程和临时结论。

## 当前锁

无活跃锁。
