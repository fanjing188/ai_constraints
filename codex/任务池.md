# 活跃任务锁

> 仅在多个活跃 Owner 且存在实际写入冲突风险时使用；Markdown 锁是例外护栏，不是常驻服务。

## 使用规则

- 单 Owner、只读子代理或 Scope 完全不重叠时不读取、不登记。
- 有冲突风险时先登记 Owner、精确写入 Scope 和状态；只允许 Owner 修改自己的任务行。
- Scope 重叠时不得并行写入，先调整边界或将冲突任务标为 `阻塞`。
- 子代理不修改本文件；由主代理登记其占用的写入 Scope。
- 完成、取消或交接后立即删除任务行；不保存需求、进度、聊天或验证日志。

## 当前锁

无活跃锁。
