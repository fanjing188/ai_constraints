# 影响分析

> 满足以下任一条件时先填写：
> 改两个及以上模块、改公开接口、改数据库 / 存储、删除或重命名已有能力、新增第三方依赖。

## 本次改动

- **任务ID**：[如 `T01`]
- **业务模块**：[模块名]
- **类型**：新增 / 修改 / 删除 / 重构
- **位置**：[文件路径 + 函数 / 模块名]
- **性质**：兼容改动 / 破坏性改动 / 仅内部实现

## 影响范围

1. **直接调用方**：[函数 / 业务模块列表]
2. **间接影响**：[调用方的调用方 / 跨模块连锁]
3. **存储影响**：[是否改 schema / 数据兼容策略]
4. **配置影响**：[是否改环境变量、配置文件、部署参数]
5. **任务冲突**：[是否与其他 `[析]`、`[做]`、`[阻]`、`[待验]` 状态任务的 Scope 冲突]
6. **验证范围**：[需要补跑哪些测试或手工路径]
7. **验收方式**：[最终由谁确认，以及按什么现象算完成]
8. **文档同步清单**：
   - [ ] `ai_constraints/codex/架构.md`
   - [ ] `ai_constraints/codex/模块/{模块名}.md`
   - [ ] 其他受影响业务模块文档
   - [ ] `ai_constraints/codex/坑点.md`
   - [ ] `ai_constraints/codex/模式.md`
   - [ ] `ai_constraints/codex/任务池.md`

## 确认项

- [ ] 范围已经收敛，没有顺手扩大
- [ ] 所有高风险调用方和受影响业务模块都已列出
- [ ] 与其他进行中任务没有未处理的 Scope 冲突
- [ ] 破坏性改动有明确迁移或同步方案
- [ ] 验证范围覆盖关键主路径和边界路径
- [ ] 任务池对应任务行已经补充风险、验证或阻塞说明
