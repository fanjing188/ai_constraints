# 项目模块地图

> 只在跨模块修改或判断系统级依赖时读取。模块内部事实由对应模块档案负责。

## 项目快照

- **项目目标**：[一句话说明产品和目标用户]
- **技术栈**：[语言、框架、存储和关键运行环境]
- **启动入口**：[主启动文件或命令]

## 模块索引

| 模块 | 代码 Scope | 职责 | 档案 |
|---|---|---|---|
| [模块名] | `src/example/` | [负责什么，不负责什么] | `src/example/AGENTS.md` |

模块跨多个目录、不适合就近放置 `AGENTS.md` 时，档案统一放到 `codex/模块/{模块名}.md`，不要同时维护两份。

## 跨模块依赖

| 上游模块 | 下游模块 | 契约或影响 |
|---|---|---|
| [模块 A] | [模块 B] | [接口、事件、数据或状态依赖] |

## 全局契约

- [只记录多个模块共同依赖、代码中不容易发现的不变量]

## 跨模块风险

- [只记录已经发生、根因明确且可能影响多个模块的问题及避免方式]

## 维护边界

- 本文件不记录模块内部流程、任务状态、临时方案和验证日志。
- 新增、拆分、合并模块，改变跨模块依赖，或确认新的跨模块风险时更新；普通模块内部修改不更新。
- 表格必须使用真实路径和稳定事实；模板落地后删除示例行。
